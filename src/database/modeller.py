# from core import entities

# from . import models


# def create_user(user: entities.User) -> models.User:
#     return models.User(name=user.name)


# def create_contact(contact: entities.Contact, owner_id: int) -> models.Contact:
#     return models.Contact(
#         owner_id=owner_id,
#         service=contact.service,
#         address=contact.service,
#     )


# def create_group(group: entities.Group) -> models.Group:
#     return models.Group(
#         name=group.name,
#         message=group.message,
#     )
